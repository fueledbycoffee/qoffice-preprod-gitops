apiVersion: apps/v1
kind: Deployment
metadata: { name: qmail, namespace: web }
spec:
  replicas: 1
  selector: { matchLabels: { app: qmail } }
  template:
    metadata: { labels: { app: qmail } }
    spec:
      containers:
        - name: qmail
          image: rg.fr-par.scw.cloud/pandormedia/qoffice-qmail-web:preprod-600d6ad56bcbfa2af9ec7d371cc9b6432696dc21
          ports: [ { containerPort: 80 } ]
          volumeMounts:
            - name: nginx-cache
              mountPath: /var/cache/nginx
            - name: nginx-run
              mountPath: /var/run
      volumes:
        - name: nginx-cache
          emptyDir: {}
        - name: nginx-run
          emptyDir: {}

apiVersion: apps/v1
kind: Deployment
metadata: { name: qmeet, namespace: web }
spec:
  replicas: 1
  selector: { matchLabels: { app: qmeet } }
  template:
    metadata: { labels: { app: qmeet } }
    spec:
      containers:
        - name: qmeet
          image: rg.fr-par.scw.cloud/pandormedia/qoffice-qmeet-web:preprod-latest
          ports: [ { containerPort: 80 } ]
          volumeMounts:
            - name: nginx-cache
              mountPath: /var/cache/nginx
            - name: nginx-run
              mountPath: /var/run
      volumes:
        - name: nginx-cache
          emptyDir: {}
        - name: nginx-run
          emptyDir: {}

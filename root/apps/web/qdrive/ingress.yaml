apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: qdrive-ingress
  namespace: web
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  ingressClassName: traefik
  tls:
    - hosts: [ "qdrive.preprod.qoffice.cloud" ]
      secretName: qdrive-tls
  rules:
    - host: qdrive.preprod.qoffice.cloud
      http:
        paths:
          - path: /
            pathType: Prefix
            backend: { service: { name: qdrive, port: { number: 80 } } }

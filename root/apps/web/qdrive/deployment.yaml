apiVersion: apps/v1
kind: Deployment
metadata: { name: qdrive, namespace: web }
spec:
  replicas: 1
  selector: { matchLabels: { app: qdrive } }
  template:
    metadata: { labels: { app: qdrive } }
    spec:
      containers:
        - name: qdrive
          image: rg.fr-par.scw.cloud/pandormedia/qoffice-qdrive:preprod-latest
          ports: [ { containerPort: 80 } ]
          volumeMounts:
            - name: nginx-cache
              mountPath: /var/cache/nginx
            - name: nginx-run
              mountPath: /var/run
      volumes:
        - name: nginx-cache
          emptyDir: {}
        - name: nginx-run
          emptyDir: {}

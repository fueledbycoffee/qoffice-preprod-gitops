apiVersion: apps/v1
kind: Deployment
metadata: { name: qai, namespace: web }
spec:
  replicas: 1
  selector: { matchLabels: { app: qai } }
  template:
    metadata: { labels: { app: qai } }
    spec:
      containers:
        - name: qai
          image: rg.fr-par.scw.cloud/pandormedia/qoffice-qai-web:preprod-latest
          ports: [ { containerPort: 80 } ]
          volumeMounts:
            - name: nginx-cache
              mountPath: /var/cache/nginx
            - name: nginx-run
              mountPath: /var/run
      volumes:
        - name: nginx-cache
          emptyDir: {}
        - name: nginx-run
          emptyDir: {}

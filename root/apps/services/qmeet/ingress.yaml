apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: qmeet-ingress
  namespace: platform
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  ingressClassName: traefik
  tls:
    - hosts: [ "qmeet.preprod.qoffice.cloud" ]
      secretName: qmeet-tls
  rules:
    - host: qmeet.preprod.qoffice.cloud
      http:
        paths:
          - path: /
            pathType: Prefix
            backend: { service: { name: qmeet, port: { number: 8080 } } }

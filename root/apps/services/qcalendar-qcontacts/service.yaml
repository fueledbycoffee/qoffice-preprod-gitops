apiVersion: v1
kind: Service
metadata: { name: qcalendar-qcontacts, namespace: platform }
spec:
  selector: { app: qcalendar-qcontacts }
  ports: [ { port: 8080, targetPort: 8080 } ]
  type: ClusterIP
